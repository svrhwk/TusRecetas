<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tusrecetas"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="13"
        android:targetSdkVersion="18" />

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="com.example.tusrecetas.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.example.tusrecetas.RecetaActivity"
            android:label="@string/title_activity_receta" >
        </activity>
        <activity
            android:name="com.example.tusrecetas.EditRecetaActivity"
            android:label="@string/title_activity_edit_receta"
            android:windowSoftInputMode="stateUnchanged|adjustPan" >
        </activity>
<!--             android:configChanges="orientation|screenSize|keyboardHidden" -->
    </application>

</manifest>
